import {createMuiTheme} from '@material-ui/core/styles';

const ThemeCRMApp = createMuiTheme({
    palette: {
        primary: {
            main: '#DF0000',
        },
        secondary: {
            main: '#333',
        },
    },
});

export default ThemeCRMApp;